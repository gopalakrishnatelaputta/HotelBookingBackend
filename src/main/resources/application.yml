spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gkhotel
    username: gopal
    password: gopal
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

# File upload settings

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

